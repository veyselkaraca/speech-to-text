import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'spech-to-text',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
